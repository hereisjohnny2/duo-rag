# --- Extração de texto / OCR ---
PyMuPDF>=1.24.0          # extração de texto nativo de PDFs (fitz)
pytesseract>=0.3.10      # wrapper para o Tesseract OCR (binário deve estar instalado no sistema)
pillow>=10.0.0           # manipulação de imagens para o OCR

# --- RAG / embeddings / vector store ---
chromadb>=0.5.0
langchain>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
ollama>=0.3.0            # cliente Python para o servidor Ollama local

# --- Interface ---
streamlit>=1.36.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --- Utilidades ---
pydantic>=2.7.0
python-dotenv>=1.0.0
tqdm>=4.66.0

# --- Empacotamento (executável Windows) ---
pyinstaller>=6.8.0
